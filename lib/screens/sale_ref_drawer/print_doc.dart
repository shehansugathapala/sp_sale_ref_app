import 'package:flutter/material.dart';

class PrintDoc extends StatelessWidget {
  PrintDoc({Key? key}) : super(key: key);

  final List<Map<String, dynamic>> data = [];

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
